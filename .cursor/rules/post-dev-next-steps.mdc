---
description: 开发完成后自动分析并输出下一步工作
alwaysApply: true
---

# 开发完成后的下一步分析

每次**完成一项开发任务**（实现功能、修复 bug、重构、联调等），在回复末尾**必须**附上「下一步工作」小节。纯问答、代码审查、仅解释原理时可省略。

## 何时触发

- 已提交代码改动、或明确宣告某任务已完成
- 用户说「完成了」「好了」「继续」等表示当前阶段结束
- 不触发：仅回答问题、用户只要 review、任务中途被新需求打断

## 分析步骤（内部执行，不必逐步写出）

1. 对照本次改动，列出仍缺失、未验证或未接入的部分
2. 结合对话中的用户目标，判断优先级
3. 检查是否需：重启服务、跑 migration、补测试、更新文档
4. 输出 **1～3 条**具体、可执行的下一步（避免空泛的「继续优化」）

## 输出格式

```markdown
## 下一步工作

1. **[优先级/类型]** 具体动作 — 简要原因
2. ...
3. （可选）...
```

- **优先级**：高 / 中 / 低，或 P0 / P1 / P2
- **类型**：验证、联调、补测试、文档、技术债、产品缺口 等
- 若当前 scope 已闭环，写「暂无必须跟进项」，并最多给 1 条可选优化

## 示例

```markdown
## 下一步工作

1. **高 · 验证** 重启后端并打开 `/ai/config`，确认「后台任务」面板有实时数据 — cron 已接入 tracker，需运行时验证
2. **中 · 补测试** 为 `TaskTrackerService` 增加单元测试 — 当前无覆盖
3. **低 · 产品** 将 esports 手动拉取也接入 task tracker — 与 cron 行为一致
```

## 原则

- 下一步应基于**本次 diff 与项目现状**，不重复用户已明确拒绝的事项
- 不要堆砌无关建议；每条都应能独立开工
- 若 build/test 未跑，下一步第一条应包含验证命令或手动检查项
