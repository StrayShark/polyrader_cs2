{
  "name": "@polyrader/infra",
  "version": "0.1.0",
  "private": true,
  "description": "Infrastructure layer - database, cache, external API clients",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@polyrader/core": "*",
    "better-sqlite3": "^11.0.0",
    "cheerio": "^1.0.0",
    "lru-cache": "^11.5.1",
    "playwright": "^1.61.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
